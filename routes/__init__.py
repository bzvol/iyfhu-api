from routes.start_donation import start_donation

# dynamically import all routes

# import os
# modules = [f[:-3] for f in os.listdir(os.path.dirname(__file__)) if f.endswith('.py') and not f.startswith('__')]
# for module in modules:
#     exec(f'from routes.{module} import {module}')
